pipeline:
  identifier: Build_noopurtibdiwal_sample_nodejs_app_1787855112989
  name: Build sample-nodejs-app
  orgIdentifier: default
  projectIdentifier: bootcampcinoopur
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: noopurtibdiwal/sample-nodejs-app
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: installdependencies
                  name: install dependencies
                  spec:
                    command: npm install
                  timeout: ""
                  type: Run
              - step:
                  identifier: dockerbuild
                  name: docker_build
                  spec:
                    caching: true
                    connectorRef: <+input>
                    repo: hello/world
                    tags:
                      - latest
                  timeout: ""
                  type: BuildAndPushDockerRegistry
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
