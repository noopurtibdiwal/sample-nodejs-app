apiVersion: v1
kind: Service
metadata:
  name: sample-nodejs-app-bg
spec:
  selector:
    app: sample-nodejs-app
    type: green
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: LoadBalancer
