apiVersion: v1
kind: Service
metadata:
  name: sample-app-canary
spec:
  selector:
    app: sample-app-canary
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: LoadBalancer
